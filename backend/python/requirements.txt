# Python 3.14 compatible dependencies
flask>=3.0.0
flask-cors>=4.0.0
numpy>=2.0.0
scikit-learn>=1.5.0
gunicorn>=22.0.0

# Optional: TensorFlow (may have limited Python 3.14 support)
# Uncomment if TensorFlow adds Python 3.14 support
# tensorflow>=2.16.0
